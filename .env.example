# Docker Registry
REGISTRY=cr.example.com

# APT-Cacher NG proxy (optional, leave empty to disable)
APT_CACHER=apt-cacher.example.com

# Docker buildx builder name for amd64 builds
AMD64_BUILDER=default

# Remote host for deployment (SSH accessible)
REMOTE_HOST=remote.example.com
